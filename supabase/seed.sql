-- Seed file intentionally minimal.
-- Schema lives in migrations. Per-user default categories are created by
-- app_private.seed_default_categories() during onboarding.
-- Deterministic test users and bulk sample data are created by test tooling
-- (see supabase/tests and tool/seed_dev_data.sql), never by this file.
select 1;
